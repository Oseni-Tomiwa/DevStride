import { PublicPage } from "../../components/public-page";
import { createPublicMetadata } from "../../lib/site-metadata";

export const metadata = createPublicMetadata(
  "DevStride Privacy",
  "Understand the account, practice, realtime, memory, and session data DevStride currently handles.",
  "/privacy",
);

export default function PrivacyPage() {
  return (
    <PublicPage className="policy-page">
      <section className="public-page-hero" aria-labelledby="privacy-title"><p className="eyebrow">Privacy</p><h1 id="privacy-title">Your practice data should stay understandable.</h1><p>This launch draft explains what DevStride currently handles. Final privacy review remains required for jurisdiction-specific notices and operational policy approval.</p></section>
      <article className="public-section public-section-surface public-prose policy-content" aria-labelledby="privacy-content-title"><h2 id="privacy-content-title">Current data practices</h2><h3>Operator and account information</h3><p>DevStride is operated by Mavery Innovative Systems LTD. The service is intended for people aged 18 and over. DevStride uses Supabase Auth for account authentication and can receive your email and account timestamps from the authenticated user record. Your coaching Profile includes display name, level, target role, preferred stack, communication goal, and feedback preference.</p><h3>Practice data</h3><p>Goals and focus areas, Conversations, Messages, structured session summaries, Progress evidence, Practice Reports, interview assessments, and timestamps are stored for the authenticated owner of that data. Bounded Memory records are user-owned, inspectable, editable, and archivable. Progress is derived from these records rather than stored as a separate user-content table.</p><h3>Microphone and camera</h3><p>Microphone audio is used for realtime AI interaction in Live Interview, Live Mentor, and Video Interview. In Video Interview, camera video is a local browser preview only: it is not uploaded to DevStride, not sent to OpenAI, not recorded by DevStride, and not persisted.</p><h3>Authentication cookies and session policy</h3><p>Next.js uses Supabase SSR authentication cookies to keep the signed-in session available across server-rendered routes. DevStride also uses signed, HttpOnly session-policy metadata for a 24-hour inactivity limit and a 7-day absolute lifetime. These are authentication/session cookies, not tracking cookies. No advertising or analytics cookies are part of the current implementation.</p><h3>Service providers</h3><p>Current architecture uses Supabase for authentication and PostgreSQL, OpenAI for backend-only AI and realtime provider operations, and the configured hosting/infrastructure providers for running the web and API applications. DevStride does not state those providers’ separate retention practices here.</p><h3>Export and deletion</h3><p>Authenticated users can download a JSON export of their account, Profile, Goals and focus areas, conversations and messages, practice reports, realtime practice metrics, and Memory. Users can also request account deletion from Account after recent authentication and a typed confirmation. Active DevStride product data is deleted in a transaction before the Supabase Auth identity is removed. Retention of narrowly required security or legal records is not implemented by this application; any future retention must be justified, disclosed, and time-bounded.</p><h3>Backups and processors</h3><p>Active-system deletion is the behavior DevStride controls directly. This service does not currently claim a backup-retention period or that every processor copy disappears instantaneously. Future infrastructure backup lifecycle decisions must be documented separately.</p><h3>Support and questions</h3><p>A public support email is not active yet. When an owner-approved address is configured, it will appear on the Support page. Do not send passwords, access tokens, API keys, or other secrets through support channels.</p><h3>What DevStride does not promise here</h3><p>This draft does not make certification, compliance, retention-duration, or legal-jurisdiction claims. Those details require explicit owner and legal approval.</p></article>
    </PublicPage>
  );
}
